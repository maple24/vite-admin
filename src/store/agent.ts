import { defineStore } from 'pinia';

export const useAgentStore = defineStore('agent', {
    state: () => {
        return {
            hostname: <string>'',
        };
    },
    getters: {

    },
    actions: {

    },
});
